# Локальный preview: cp .dev.vars.example .dev.vars
NEXTJS_ENV=development
ADMIN_SECRET=

# D1 — wrangler.jsonc (DB). Медиа — R2 bucket csutility-media (MEDIA).
# Vercel Blob не используется. После деплоя: npm run db:fix-blob-urls
