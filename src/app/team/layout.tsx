import TeamLayoutShell from '@/components/team/TeamLayoutShell'

export default function TeamLayout({ children }: { children: React.ReactNode }) {
  return <TeamLayoutShell>{children}</TeamLayoutShell>
}
